# CSS

##CSS (Cascading Style Sheets) :

CSS is a stylesheet language used to control the appearance, layout, and design of HTML web pages. It separates the content (HTML) from the presentation (CSS), making websites more attractive, responsive, and easier to maintain.
